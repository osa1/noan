{% autoescape off %}{{ email }} wants to notify you that the following packages may be out-of-date:

{% for p in packages %}
* {{ p.name }} {{ p.full_version }} [{{ p.repo.name|lower }}] ({{ p.architecture.name }}): {{ p.get_full_homepage }}{% endfor %}

{% if message %}
The user provided the following additional text:

{{ message }}
{% endif %}{% endautoescape %}
